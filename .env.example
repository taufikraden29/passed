# Template for local environment variables configuration.
# Copy this file to .env and fill in the VITE_MASTER_PASSWORD to enable Auto-Unlock.
# NOTE: Variables prefixed with VITE_ are bundled into production build. Keep this offline-only.

VITE_MASTER_PASSWORD=""

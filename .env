# WARNING: Storing secrets in Vite environment variables prefixed with VITE_ 
# will bundle them into the compiled JavaScript. Anyone inspecting the code 
# in production can see it. Use this primarily for local/offline setups.

VITE_MASTER_PASSWORD="radenkun"
